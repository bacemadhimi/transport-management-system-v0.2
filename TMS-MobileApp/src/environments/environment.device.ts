/**
 * Environment configuration for production server
 * Configured to use the public API server
 */
export const environment = {
  production: true,
  // Public API server
  apiUrl: 'http://51.178.65.32:45880',
  weatherApiKey: '' // OpenWeatherMap API key (optional)
};
